<p id="notice"><%= notice %></p>

<h1>Bookstores</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bookstores.each do |bookstore| %>
      <tr>
        <td><%= bookstore.name %></td>
        <td><%= bookstore.description %></td>
        <td><%= bookstore.latitude %></td>
        <td><%= bookstore.longitude %></td>
        <td><%= link_to 'Show', bookstore %></td>
        <td><%= link_to 'Edit', edit_bookstore_path(bookstore) %></td>
        <td><%= link_to 'Remove', bookstore, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bookstore', new_bookstore_path %>
